@model IEnumerable<CLEANSHOP.Models.Type>

<h3></h3>
<hr />
<div class="dropdown a">
    <h4 type="button" id="dropdownMenuButton" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
        DANH MỤC &#9776;
    </h4>

    <div class="dropdown-menu b" aria-labelledby="dropdownMenuButton" style= "text-align:left;background-color :lightcoral">
        @foreach (var item in Model)
        {
        <a href="@Url.Action("SPTheoLoai", "DichVu", new { id = item.ID_Type })" class="dropdown-item" style="margin-left: 5px; text-decoration: none; color: black; background-color: lightcoral">
            &#10149;	 @item.NameType
            <hr />
        </a>
        }
    </div>
</div>
<hr />
<style>
    .a {
        width: 200px;
    }

        .a:hover .b {
            display: flex;
            flex-direction: column;
            justify-content: flex-start;
        }
</style>
